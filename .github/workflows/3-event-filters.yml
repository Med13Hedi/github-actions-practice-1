name: Event Filters
on:
  push:
    branches:
      - dev
      - 'release/*'
    paths-ignore:
      - 'docs/**'

jobs:
  test-filters:
    name: Test Filters
    runs-on: ubuntu-latest
    steps:
      - name: Confirm filters worked
        runs: echo "Event filters working! Triggered on ${{ github.ref_name }} branch"
